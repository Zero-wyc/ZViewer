# 前端 Socket.IO / API 基础地址
# 本地开发建议使用 "/"，通过 Vite 代理转发到后端；
# 若遇到 /api/stream/resolve-bilibili 等 NDJSON 流式接口被异常中断，
# 可在 .env.development 中设置为 http://localhost:3000 直接访问后端。
# 生产环境留空或不设置时，默认使用当前页面域名（适用于 Nginx 反向代理）。
VITE_API_URL=/

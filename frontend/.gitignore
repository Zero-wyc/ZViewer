# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# vendored mediabunny fork 的源码必须入库（依赖声明为 file:vendor/mediabunny，
# CI 上 npm ci + vite 构建都需要它）；上面的 `dist` 规则会误伤这里的 dist，
# 用反忽略放行
!vendor/mediabunny/dist/
!vendor/mediabunny/dist/**

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
